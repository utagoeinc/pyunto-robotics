# Copy to .env and fill in. .env is gitignored — never commit real credentials.

# Pyunto account the robot logs in as
PYUNTO_EMAIL=tom.sonoda+robot@gmail.com
PYUNTO_PASSWORD=

# Production server
PYUNTO_BASE_URL=https://api.pyunto.com

# Optional: invite code to join a human's chat space on first run
# (generate it in the Pyunto iOS/Android app)
PYUNTO_INVITE_CODE=

# Optional: restrict the robot to a single chat space (UUID).
# If unset, the robot listens on every space it is a member of.
PYUNTO_CHAT_SPACE_ID=
